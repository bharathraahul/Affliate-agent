services:
  - type: web
    runtime: static
    name: heyoddete
    rootDir: web
    buildCommand: npm start
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
